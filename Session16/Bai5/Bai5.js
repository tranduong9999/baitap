let firstNumber = prompt ("Hay nhap so dau tien");
let secondtNumber = prompt ("Hay nhap so thu hai");

if (firstNumber % secondtNumber == 0) {
    alert ("so thu nhat la boi so cua so thu hai")
} else {
    alert ("so thu nhat ko phai la boi so cua so thu hai")
}